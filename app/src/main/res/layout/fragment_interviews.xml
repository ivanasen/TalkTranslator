<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/fragment_interviews"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context="talktranslator.app.ivanasen.talktranslator.fragments.InterviewExplorerFragment">

    <android.support.v7.widget.RecyclerView
        android:id="@+id/interviews_recyclerview"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <TextView
        android:id="@+id/empty_interviews_view"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:background="@drawable/rounded_view"
        android:gravity="center"
        android:paddingBottom="@dimen/default_padding"
        android:paddingLeft="8dp"
        android:paddingRight="8dp"
        android:paddingTop="@dimen/default_padding"
        android:text="@string/empty_interviews_text"
        android:textColor="@color/materialRed"
        android:textSize="20sp"
        android:visibility="gone"/>
</FrameLayout>